{
  "description": "Code standards enforcement hooks - triggers analysis after file modifications when enabled",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/check-code-standards.sh",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "note": "Uses deterministic bash script instead of prompt-based hook for reliable behavior on non-source files."
}
